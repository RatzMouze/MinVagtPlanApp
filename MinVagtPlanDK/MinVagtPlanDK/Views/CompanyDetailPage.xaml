<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:viewmodels="clr-namespace:MinVagtPlanDK.ViewModels" x:DataType="viewmodels:CompanyDetailViewModel"
             x:Class="MinVagtPlanDK.Views.CompanyDetailPage"
             Title="{Binding Title}">
    <ScrollView>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="{Binding _Company.name}" FontSize="Large"/>
            <Label Text="Beskrivelse:" FontSize="Medium" />
            <Label Text="{Binding _Company.description}" FontSize="Small"/>
            <Label Text="Adresse" FontSize="Medium" />
            <Label Text="{Binding _Company.address}" FontSize="Small"/>
            <Label Text="Postnummer:" FontSize="Medium" />
            <Label Text="{Binding _Company.zipcode}" FontSize="Small"/>
            <Label Text="By:" FontSize="Medium" />
            <Label Text="{Binding _Company.city}" FontSize="Small"/>
            <Label Text="Land:" FontSize="Medium" />
            <Label Text="{Binding _Company.country}" FontSize="Small"/>
        </StackLayout>
    </ScrollView>

</ContentPage>